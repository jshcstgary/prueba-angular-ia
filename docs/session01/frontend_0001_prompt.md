### User Input

Crea una aplcaición de Angular en la ruta actual siguiendo las siguientes instrucciones:

Usa Angular CLI con ng para crear el proyecto
No uses standalone, la aplicación tiene que ser con módulos
Crea las rutas
Los estilos se manejarán con CSS
Que los componentes no se creen con un archivo CSS, los estilos serán en línea
No instales los paquetes de Node
Omite el primer commit para luego agregar un mensaje personalizado
Elimina el caracter caret (^) y virgulilla (~) que está al inicio del número de versión de los paquetes en el package.json para usar versiones fijas
Haz referencia a los íconos de Google en el index.html
Instala los paquetes de Node usando NPM
Instala el paquete msw con NPM en dependencias de desarrollo y usa la bandera -E
Modifica los scripts del package.json para que utilicen pnpm dlx @angular/cli en lugar de ng
Realiza el primer commit con el mensaje "Project initialization"

*Listed directory [prueba-angular](file:///c:/Users/mcastillo/Documents/prueba-angular) *

*User accepted the command `ng --version`*

*User accepted the command `npx -y @angular/cli new --help`*

*User accepted the command `powershell -ExecutionPolicy Bypass -Command "npx -y @angular/cli new --help"`*

*Checked command status*

*Checked command status*
